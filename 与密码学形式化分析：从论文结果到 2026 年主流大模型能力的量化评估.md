# CrypFormBench 与密码学形式化分析：从论文结果到 2026 年主流大模型能力的量化评估

## 核心判断

结合 CrypFormBench 论文正文、作者公开代码仓库、论文补充实验，以及截至 **2026 年 9 月 3 日** OpenAI、Anthropic、Google、DeepSeek、xAI 的最新官方模型资料，我认为这篇短文的主结论是正确的，但需要做三个重要修正。

第一，**现在的大模型并不是“缺乏密码学知识”，而是缺乏把已有知识稳定转化为“可执行、语义正确、攻击者模型正确、验证目标正确”的形式化模型的能力**。CrypFormBench 中最强模型对已有形式代码的解释已经达到 90 分以上，但从自然语言重新生成代码、尤其是跨验证器转换时，性能会坍塌一个数量级。论文的 9 模型平均分分别约为：解释 **75.1**、补全 **45.7**、纠错 **44.3**、生成 **8.0**、转换 **1.5**。这不是典型的“什么都不知道”，而是明显的 **knowledge–execution gap（知识—执行鸿沟）**。citeturn24view2turn24view3

第二，新闻中“生成、转换、纠错都明显较弱”说得过于笼统。**真正断崖式薄弱的是从零生成和跨语言转换；纠错处于中间，而且编译错误修复与语义错误修复是两件完全不同的事。** Claude-3.5-Sonnet-Coder 的纠错分实际上达到 **71.2**，DeepSeek-Coder 为 **63.9**，而 Claude 的生成只有 **24.2**、转换只有 **13.3**。论文特意把“语义错误修复”赋予 60% 的纠错权重，就是因为它明显比编译错误修复困难。citeturn24view1turn24view2

第三，新闻所说的“700 个测试实例”严格来说是 **700 个经过验证的参考形式模型文件**，并不等于只有 700 次模型问答。论文由这 700 个参考文件进一步构造了 280 个解释实例、700 个生成实例、700 个补全实例、100 个转换实例和 280 个纠错实例，总计约 **2,060 个任务级实例**。因此它比“700 道题”更接近一个从参考模型扩展出的多任务评测体系。citeturn17view0

从工程可用性上，我的最终量化判断是：

| 能力层级 | 2026 年前沿模型状态 | 是否足够 |
|---|---:|---|
| 阅读、解释已有 SPDL/SPTHY/PV/CV/EC 等形式代码 | 前沿模型已接近高水平，论文最高约 94 | **基本足够作为助手** |
| 在已有上下文中补局部形式代码 | 最强约 74，且可执行后正确率通常很高 | **部分足够，必须验证器兜底** |
| 根据编译器报错局部修复 | 最强约 71 | **可实用化，但不能直接信任结果** |
| 从自然语言独立生成完整形式模型 | 最强仅 24.2；Claude 可分析率只有 15% | **明显不足** |
| 跨 Scyther/Tamarin/ProVerif/CryptoVerif/EasyCrypt 等语义转换 | 最强仅 13.3，其余基准模型几乎全为 0 | **严重不足** |
| 模型自己判断协议是否安全、替代形式验证器 | 其他 2026 年直接研究也显示攻击漏报和过度自信 | **不应使用** |
| LLM + verifier + 多轮修复 + 检索示例 | 论文实验显示明显提高可执行率 | **当前最现实路线** |

换言之，**LLM 已经接近“形式化密码分析工程师的智能编辑器/副驾驶”，但距离“独立密码学形式化分析师”仍有实质性距离。** CrypFormBench 作者自己的结论也是：模型适合解释和局部纠错，尚不足以可靠完成完整生成和跨语言转换。citeturn24view3


## 论文究竟测了什么，以及短文中容易被忽略的细节

CrypFormBench 的技术背景确实可以概括为“符号安全分析”和“计算安全证明”两条路线，但论文实际覆盖范围比短文列举的 Scyther、Tamarin、CryptoVerif、EasyCrypt 更大。它纳入了七个验证工具/语言族：Scyther/SPDL、Tamarin/SPTHY、AVISPA/HLPSL、ProVerif/PV-HORN 系列、Maude-NPA/MAUDE、CryptoVerif/CV-OCV 和 EasyCrypt/EC。前五者主要覆盖符号协议分析及不同的代数、状态与等价推理，CryptoVerif 和 EasyCrypt则进入计算安全证明范式。citeturn17view0

这一区别非常重要。让模型“写一段看起来像密码协议的代码”并不是任务本身。一个真正合格的形式模型至少同时受到以下约束：语法必须被目标工具接受；消息流、角色绑定、随机数/新鲜性必须正确；攻击者能力必须与预期威胁模型一致；secrecy、authentication、equivalence、IND-CPA/CCA 等安全目标必须正确编码；最后验证器的 SAFE/UNSAFE 输出还必须与参考模型在同一安全假设下保持一致。论文因此把 **可分析率 analyzability \(a\)** 和 **可分析输出上的正确率 ACC\(_A\)/F1\(_A\)** 分开统计，而不是简单用字符串相似度评价代码。citeturn24view0turn24view1

数据集的筛选过程也相当严格。研究者先收集 **4,549** 个形式模型文件，去重和基本检查后剩 **3,590** 个；实际运行对应验证器后得到 1,280 个 safe、557 个 unsafe、972 个 error、781 个 timeout，然后从可分析部分中每种工具抽取 100 个代表实例，最终形成 **700 个参考文件，其中 415 safe、285 unsafe**。这 700 个文件对应 **677 个不同密码方案**。citeturn17view0

安全属性也不是只有 secrecy 和 authentication。研究团队把 241 个原始、工具相关的目标去重归一为 **160 种安全属性**，包括 secrecy、authentication、integrity、correctness、equivalence，以及长尾的 forward secrecy、IND-CPA/CCA、zero-knowledge 等；攻击者模型则包含 Dolev–Yao、CK、eCK 以及 KCI、密钥恢复、重放、中间人等应用相关攻击能力。复杂协议平均可包含两三个验证属性，有些超过十个。citeturn17view0

所以 CrypFormBench 的难点不是“是否记住 Tamarin 语法”，而是同时在测试：

> **密码学领域知识 + 形式语言知识 + 协议语义抽象 + 攻击者建模 + 安全属性建模 + 工具执行行为。**

这也是为什么普通代码能力很强的模型依然会失败。

论文的总体分数也值得理解。对于生成、补全、转换等可执行任务，核心分数形式为

\[
S_t=\operatorname{HM}\bigl(a,\operatorname{HM}(ACC_A,F1_A)\bigr),
\]

也就是说，可执行率和执行后的正确性任何一边很低，调和平均都会把总分显著拉低。最终任务权重为：解释 15%、生成 25%、补全 20%、转换 25%、纠错 15%。作者有意给生成和转换更高权重，因为它们更接近真正从设计描述获得可用形式模型的核心自动化能力。citeturn24view1

作者还用不同权重重新计算过排行榜，Spearman 排名相关性至少 **0.98**、Kendall τ 至少 **0.94**。因此 Claude 第一、生成/转换最弱这一结论并不是某组人为权重“做出来”的。citeturn24view4


## 原始结果重算：模型到底是“不会密码学”，还是“会但做不出来”

论文主表的完整结果比新闻里的一个“48.7”更有解释力。citeturn24view2

| 模型 | 生成 | 补全 | 纠错 | 转换 | 解释 | 综合 |
|---|---:|---:|---:|---:|---:|---:|
| Claude-3.5-Sonnet-Coder | **24.2** | **74.4** | **71.2** | **13.3** | 91.7 | **48.7** |
| DeepSeek-Coder | 5.8 | 68.0 | 63.9 | 0.0 | 93.2 | 38.6 |
| GPT-4o | 9.5 | 51.5 | 54.7 | 0.0 | **94.1** | 35.0 |
| DeepSeek-R1 | 5.8 | 65.2 | 56.9 | 0.0 | 77.4 | 34.6 |
| LLaMA4-Instruct | 5.8 | 55.6 | 57.2 | 0.0 | 44.4 | 27.8 |
| GPT-4o-mini | 0.0 | 10.9 | 44.8 | 0.0 | 93.8 | 23.0 |
| Gemini-2.5-Pro | 7.7 | 25.9 | 35.6 | 0.0 | 69.1 | 22.8 |
| Grok-3 | 12.8 | 36.7 | 0.0 | 0.0 | 49.6 | 18.0 |
| GLM-4 | 0.0 | 22.8 | 14.5 | 0.0 | 62.3 | 16.1 |

基于这张表，我进一步做了一个**不是论文官方指标、而是用于判断“知识是否够”的二次分析**。把“解释 + 补全”的平均值定义为 **知识访问/上下文利用指标 K**，因为这两个任务给模型较多现成结构；把“生成 + 转换”的平均值定义为 **自主构造指标 C**；纠错单独作为修复能力 R。

| 模型 | K：理解/上下文利用 | C：自主构造 | R：纠错 |
|---|---:|---:|---:|
| Claude-3.5 | **83.1** | **18.8** | 71.2 |
| DeepSeek-Coder | **80.6** | 2.9 | 63.9 |
| GPT-4o | 72.8 | 4.8 | 54.7 |
| 九模型平均 | **60.4** | **4.7** | 44.3 |

这些数字来自论文表 3 的算术重组，而不是新的模型测试。citeturn24view2

这个结果非常有辨识力：九模型平均的“有上下文理解能力”是 **60.4**，自主构造却只有 **4.7**，相差约 **55.7 个百分点、约 12.8 倍**。Claude 的对应值是 83.1 对 18.8，仍有 **64.3 分**的巨大落差。

因此，“知识不够”并不是最准确的诊断。更精确的结论是：

**模型内部显然已经包含相当多密码协议和形式语言的陈述性知识，但这些知识尚未形成稳定的、可组合的程序化形式建模能力。**

Claude 的生成数据尤其能说明这一点。它的生成可分析率只有 **15%**，但在已经能够分析的输出上，ACC\(_A\)=0.62、F1\(_A\)=0.63。也就是说，它并非完全不知道该怎么建模，而是大部分答案根本无法可靠进入验证器。补全任务更明显：可分析率为 62%，而一旦可分析，ACC\(_A\) 和 F1\(_A\) 都达到 **0.93**。citeturn24view2

把论文调和平均公式进一步拆开，会得到一个非常有价值的反事实分析。对于 Claude：

- 生成当前 \(a=0.15\)，条件质量 \(HM(0.62,0.63)\approx0.625\)，所以得分 24.2；假设**只把可分析率提升至 100%，语义正确性完全不变**，生成理论得分可升至约 **76.9**。
- 补全的同样反事实上限约为 **96.4**，说明补全几乎主要是“能不能稳定写出工具接受的东西”的问题。
- 转换则完全不同。其 \(a=0.14\)、ACC\(_A\)=0.07、F1\(_A\)=0.62；即便假设可分析率神奇地提高到 100%，按同样公式算，转换得分也只有约 **22.3**。citeturn24view1turn24view2

这意味着**生成主要受 executability/grammar/scaffolding 限制，而转换已经进入真正的语义映射瓶颈**。跨工具时模型不能只“学会语法”，还必须理解不同工具对状态、攻击者、等价关系、概率实验、随机预言机、事件与查询的不同抽象方式。

进一步地，如果保持 Claude 当前解释和纠错能力不变，只假定生成、补全和转换的 **可分析率全部提高到 100%**，而它们可分析后的语义质量保持原状，按论文权重计算，综合分大约也只会从 **48.7 上升到 68.5**。这说明把所有 parser/compiler 错误消灭虽然很重要，但**仍然不足以得到可靠的自动形式化分析系统**。这个 68.5 是基于论文数据的反事实计算，不是作者报告的实测分数。citeturn24view1turn24view2


## 为什么模型“看得懂”却“写不对”：真正的能力瓶颈

CrypFormBench 的失败模式可以归纳成几个不同层次。

最浅的一层是**形式语言脚手架问题**。模型会漏 import/module、声明、角色环境块、event/query，变量未定义，或者混用另一种验证器的语法。论文的 few-shot 实验对此给出了很直接的证据：在 10% 子集上，从 zero-shot 增加到 3-shot 后，平均生成可分析率从 **6.6% 提高到 24.9%**，约为原来的 **3.8 倍**；补全从 **42.9% 提高到 50.9%**，同时正确率仍保持在 92% 以上。作者指出，示例主要帮助模型复现 imports、modules、role/environment blocks、event/query declarations 等形式化脚手架。citeturn24view5

第二层是**安全语义问题**。形式模型“能运行”并不代表“建模的是同一个协议”。错误的新鲜性条件、角色身份绑定、攻击者能力、事件匹配和 correspondence property 都可能使验证器给出一个完全合法、但针对错误模型的 SAFE。论文也正因此同时统计 analyzability 与安全判定正确性，而不是把“编译通过”作为成功。citeturn24view0turn17view0

第三层是**跨验证范式的语义迁移**。例如：

- SPDL 中的消息序列并不能机械翻译成 Tamarin 的 multiset-rewriting 规则；
- ProVerif 的 applied-π calculus / Horn abstraction 与 Tamarin 的显式状态和 trace reasoning 并非简单语法替换；
- CryptoVerif 与 EasyCrypt 更不是把“symbolic protocol code”换一种关键字，而涉及 computational games、概率程序、归约与困难性假设。

论文结果恰好印证这一点：基准中除了 Claude 的转换分 13.3 外，其他八个模型的转换总分全部为 **0**。citeturn19view0turn24view2

第四层是**长时工具交互和自我修复能力**。一份人类形式模型往往不是一次写成，而是“生成 → parser/compiler → 根据错误修改 → verifier → 查看 trace/goal → 修正攻击者模型或 security claim → 再验证”的循环。论文发现一旦模型真正生成了可分析脚本，其运行时间和文件规模通常已经接近人工参考模型；真正最大的瓶颈仍然是模型能否首先产生正确的、验证器友好的结构。Scyther/Tamarin 的典型验证时间常在约 20 秒以内，而复杂 EasyCrypt/Maude-NPA 实例可能超过约 400 秒，因此代理还必须理解 timeout 与验证器行为，而不仅是写代码。citeturn24view4

这也解释了为什么论文提出的方向非常合理：**grammar-constrained generation、schema-guided generation、统一中间表示 IR、verifier-in-the-loop repair** 比单纯把基础模型参数做得更大更有针对性。论文自己的轻量 LoRA 实验也说明了这一点：Qwen2.5-Coder-3B 微调后，补全任务可分析输出从 5 个升至 23 个，但生成只从 4 升至 5，转换仍然是 0。citeturn24view5

还有一个不能忽视的评测边界：CrypFormBench 的主要安全标签是**scheme-level**，即某一安全目标违反就把整个方案标成 UNSAFE。作者另外进行细粒度 property-level 评测后发现，生成下降最大，补全和纠错平均也会出现约 10% 的下降，而解释排序基本稳定。换句话说，主表实际上可能对“真正把每个安全属性都建模正确”的能力略有乐观。citeturn4view2

作者同样承认数据污染无法被完全排除：Needham–Schroeder、TLS/SSH 类握手、5G-AKA 等标准协议的描述或部分形式模型可能存在于互联网和预训练语料中。但模型即使可能见过这些协议，跨工具转换和语义纠错依然持续失败，因此作者认为结果主要体现真实能力差距，而非单纯记忆 benchmark。citeturn17view0


## 2026 年主流模型已经强了很多，但不能直接把通用 benchmark 分数换算成 CrypFormBench 分数

这里必须严格区分两个问题：

> **“2026 年模型是否比论文主表中的 GPT-4o、Claude-3.5、Gemini-2.5-Pro、Grok-3 更强？”**

答案显然是是。

> **“因此现在 GPT-5.6、Claude Opus 5、Gemini 3.1 Pro 等已经可以独立完成密码学形式验证吗？”**

公开证据并不支持这个推论。

截至 2026 年 9 月 3 日，CrypFormBench 官方 GitHub 主榜仍然列出论文中的 9 个模型；论文正文另外加入了一个 GPT-5.1 扩展实验，但我没有在官方论文或公开仓库中发现 GPT-5.6 Sol、Claude Opus 5、Gemini 3.1 Pro、DeepSeek V4-Pro、Grok 4.6 的 CrypFormBench 完整结果。因此，给这些最新模型编造一个“预计 62 分”“预计 70 分”之类的数字，在研究上是不成立的。citeturn19view0turn17view0

当前主要高端模型的公开状态可以这样理解：

| 当前代表模型 | 2026 年官方公开能力证据 | 与 CrypFormBench 的关系 |
|---|---|---|
| **GPT-5.6 Sol** | OpenAI 2026-07-09 发布的旗舰；官方报告 GPQA Diamond **94.6%**、FrontierMath v2 T1–3 **89%**、CTF **96.7%**、SEC-Bench Pro **71.2%**，Toolathlon **58.0%**、AutomationBench **18.1%**。citeturn21search1 | 数学、代码、安全能力明显比 GPT-4o 时代强，但工具自动化仍明显比知识型 benchmark 难；**没有 CFB 实测** |
| **Claude Opus 5 / Fable 5 系列** | Anthropic 2026-07-24 发布 Opus 5，官方将其定位为长时 agentic coding 和 knowledge work 模型，并强调更强的自我验证与迭代；Opus 5 在多项软件工程/知识工作评测上超过 Opus 4.8，能力接近更高端的 Fable 5。citeturn23view0 | Claude-3.5 是原 CFB 第一，因此新 Claude 是最值得重新评测的候选之一；但 **尚无公开 CFB 分数** |
| **Gemini 3.1 Pro** | Google 2026-02-19 发布；官方模型卡报告 Humanity’s Last Exam 无工具 **44.4%**、Search+Code **51.4%**，并显示对复杂推理相比 Gemini 3 Pro 有明显提升。citeturn20search2turn20search5 | 其前身 Gemini-2.5-Pro 在 CFB 只有 **22.8**，说明通用推理强并不能自动解决形式 DSL；**3.1 尚无 CFB 实测** |
| **DeepSeek V4-Pro** | DeepSeek 官方 API 已提供 V4-Pro，支持 1M 上下文以及可调思考模式；官方更新记录列出 V4-Pro 2026-08-13 GA。citeturn22search9turn22search21 | DeepSeek-Coder 在 CFB 达 **38.6**、高于 R1 的 34.6，反而说明代码/DSL 专门化可能比纯推理扩展更重要；**V4-Pro 尚无 CFB 实测** |
| **Grok 4.6** | xAI 2026-08-12 发布，重点提升长时代理和代码工作流，并报告其 Artificial Analysis Intelligence Index 可与 GPT-5.6 Sol 匹配。citeturn22search3 | Grok-3 在 CFB 仅 **18.0**；4.6 的 agent 能力可能特别有利于 verifier-in-loop，但 **没有直接 CFB 数据** |

这里最有价值的“跨代实测”其实来自论文自己后来补做的 **GPT-5.1**。作者把 GPT-5.1 插入同一套 CrypFormBench 流水线后，OpenAI 系列综合分从：

\[
\text{GPT-4o-mini }23.0
\rightarrow
\text{GPT-4o }35.0
\rightarrow
\text{GPT-5.1 }39.2
\]

有所提高，但 GPT-5.1 仍然保持同样的能力轮廓：解释/补全较强，生成/转换仍受低可分析率和 timeout 限制。GPT-5.1 比 GPT-4o 高 **4.2 分，约 12% 相对增长**，却仍比 2024 代 Claude-3.5 的 48.7 低 **9.5 分**。这是目前最直接的证据，表明**通用模型规模和推理能力升级不会自动消除高度专业化形式语言的缺陷**。citeturn17view0

OpenAI 最新的 GPT-5.6 Sol 在通用数学、安全、代码和 agent benchmark 上已经远强于 GPT-4o 世代，Anthropic Opus 5 也专门强化了多轮验证、根因定位和长时工作流。citeturn21search1turn23view0 从这些能力变化推断，**2026 前沿模型很可能显著提高 CrypFormBench 中的 analyzability、纠错和 verifier-in-loop 性能**，这是合理推断；但“它们因此已经超过 70/80 分”则没有公开实验依据。

更值得警惕的是，OpenAI 自己的 GPT-5.6 数据就呈现了类似的“知识/推理分很高，但端到端工具任务低得多”的现象：GPQA Diamond 为 94.6%、CTF 为 96.7%，而 AutomationBench 只有 18.1%、Toolathlon 58.0%。这些 benchmark 与 CrypFormBench 不能直接横向比较，但能力形态非常相似：**知道答案和完成一个长链、外部工具约束下的正确工作流不是同一种能力。** citeturn21search1


## “是否具备足够密码学知识”的最终量化判定

要回答用户真正关心的“这些模型是否已经具备足够知识”，我建议不要把“知识”看成一个二元变量，而分成五层。

### 密码学概念与协议识别知识：高

Claude-3.5 的 CrypFormBench 解释分为 91.7，GPT-4o 94.1，DeepSeek-Coder 93.2；GPT-4o 在可分析解释样本上的 ACC\(_A\)/F1\(_A\) 甚至达到 1.00/1.00。论文还对 embedding 相似度做了 BGE、E5 交叉验证和人工抽查，模型排序总体稳定。citeturn24view2turn24view4turn24view5

因此，对于：

“这是什么协议？”
“这一行 Tamarin rule 在做什么？”
“这个 ProVerif query 表示什么安全目标？”
“这里的 nonce、session key、role binding 在协议中分别是什么？”

**前沿模型已经具备相当充足的基础知识。**

### 目标形式语言局部语法知识：中高，但不稳定

Claude 补全 74.4、DeepSeek-Coder 68.0；而 Claude 的可分析输出一旦真正通过工具，ACC\(_A\)=93%。few-shot 又能把生成可分析率从 6.6% 提高到 24.9%。这些现象说明模型“知道不少语法”，只是不能稳定从参数中调用正确的 DSL 结构。citeturn24view2turn24view5

因此局部代码补全、模板扩写属于**可以使用，但必须跑真实 verifier** 的能力。

### 从自然语言抽象成正确安全模型的知识：不足

Claude 从零生成只有 24.2，而且可分析率只有 15%。即使通过了语法关，它的条件准确率也只是约 62%。citeturn24view2

这说明模型容易漏掉恰恰决定安全性的细节：

\[
\text{freshness}
,\quad
\text{role binding}
,\quad
\text{attacker capability}
,\quad
\text{session binding}
,\quad
\text{security goals}.
\]

一段程序中漏一个普通变量可能让单元测试失败；一段安全模型中漏一个 attacker capability 却可能更危险——**验证器会非常严谨地证明一个错误模型是安全的。**

### 跨形式系统的语义知识：严重不足

Claude 的转换 13.3，其余八个主表模型为 0。citeturn19view0turn24view2

这是当前最强的反证：如果模型真正形成了稳固的“协议语义中间表示”，那么从 SPDL→SPTHY、PV→CV 等转换不应该如此彻底崩溃。结果更像是模型大量依赖目标语言模板、表面模式和局部训练记忆，而不是拥有一个可以自由投射到不同形式系统的完整语义模型。

### 独立作出安全结论的能力：不够，而且风险最高

这一点还有 CrypFormBench 之外的直接证据。2026 年另一项针对符号安全协议分析的研究《Evaluating Large Language Models for Symbolic Security Protocol Analysis》用 **130 个经过混淆的 AnB/AnBx 协议、388 个安全目标**测试 GPT 和 DeepSeek 系列直接判断协议安全性。聊天模型对攻击的 recall 可以达到约 69–81%，但 precision 低于 31%；使用 reasoning 后 precision 可以提高，例如 GPT 系列达到约 66.5%，DeepSeek reasoning 约 45.4%，但仍只发现略多于一半的攻击。更危险的是，模型在漏掉攻击时仍经常给出约 94–99% 的高置信度。研究结论同样是不应把模型当成形式验证器，而更适合作为预分析工具。citeturn14search0turn15view2turn15view3

因此，对“是否有足够知识”的最准确回答不是简单的“有/没有”，而是：

> **有足够知识参与形式化分析，没有足够的可靠程序化知识独立承担形式化分析。**

这两句话看似只差几个字，但实际对应完全不同的工程使用方式。


## 从 48.7 到实际可用：最现实的路线不是等下一代模型，而是改变系统结构

CrypFormBench 的结果其实已经给出了相当清楚的工程路线。

最不应该采用的是：

\[
\text{Protocol PDF}
\rightarrow
\text{LLM}
\rightarrow
\text{Tamarin/EasyCrypt code}
\rightarrow
\text{接受最终结论}
\]

因为这条链上只要 LLM 错误地省略一个攻击者能力或安全目标，后面的验证器反而会把一个错误模型“严格证明”出来。

更合理的是：

\[
\text{协议描述}
\rightarrow
\boxed{\text{结构化 IR}}
\rightarrow
\text{LLM 生成目标 DSL}
\rightarrow
\text{Parser/Type Checker}
\rightarrow
\text{Verifier}
\rightarrow
\text{Trace/Errors}
\rightarrow
\text{LLM Repair}
\rightarrow \cdots
\rightarrow
\text{人工审计安全假设}.
\]

论文自己的 few-shot 数据支持这种系统化思路：只提供三个目标语言示例，就能把生成可分析率从 6.6% 拉到 24.9%；这意味着模板检索/RAG、语法约束解码和 verifier feedback 的收益可能比单纯换一个更大的基础模型更直接。citeturn24view5

按照 CrypFormBench 的数据，我会把实际部署划成如下三个档位。这里的门槛是基于安全关键软件的工程判据，而不是论文官方评级：

| 使用模式 | 建议 | 原因 |
|---|---|---|
| **解释代码、生成注释、解释攻击 trace、辅助文档** | 可以现在部署 | 顶级解释分已 >90，风险主要是解释错误，可由专家复核 |
| **补全、根据编译错误修代码、生成协议初稿** | 可部署为 human-in-the-loop | 最强补全/纠错 70+，但必须让真实工具执行并复查 security goals |
| **自主从论文生成完整模型并宣布 SAFE/UNSAFE** | 不应部署 | 生成 24.2、转换 13.3，攻击者/属性语义错误可能被验证器“合法证明” |
| **跨验证器自动迁移并作为正式安全证明依据** | 当前尤其不适合 | 基准转换几乎全线失败 |
| **LLM agent + verifier 多轮迭代** | 当前最有潜力 | 正好针对最大瓶颈 analyzability，且新一代模型的长时 agent/tool use 能力明显增强。citeturn23view0turn21search1 |

从量化角度看，Claude-3.5 的 48.7 里，解释/补全/纠错已经贡献了相当多分数，而生成和转换恰好合计占 CrypFormBench **50% 权重**。以 Claude 当前三个较容易任务分数固定计算，要把总体分提高到 **70**，生成与转换两项的平均分需要从当前 **18.75** 提高到约 **61.4**；要到 **80**，则需要约 **81.4**。而如果解释、补全、纠错完全不进步，即使生成和转换全部满分，Claude 的理论总分也只有约 **89.3**。这些都是依据论文权重的算术反推。citeturn24view1turn24view2

因此未来真正的“及格线”不是 GPT-5.6 或 Claude Opus 5 能否把 48.7 提到 55，而是是否出现下面这种结构性变化：

\[
a_{\text{generation}}:
15\%\rightarrow 90\%+
\]

\[
a_{\text{transformation}}:
14\%\rightarrow 90\%+
\]

同时 transformation 的条件语义正确率还必须从 Claude 当前非常低的水平大幅提升。只有可分析率和语义正确性**同时**接近工程级可靠性，模型才真正跨过“辅助工具”到“自动形式化建模系统”的边界。论文所采用调和平均而不是普通平均，正是为了阻止模型靠其中一个指标很高来掩盖另一个指标的崩溃。citeturn24view1turn24view2


## 综合结论与对这篇新闻的修订版解读

CrypFormBench 最重要的发现，不应概括成简单的“LLM 不懂密码学”。

更准确的结论是：

**当前 LLM 已经拥有大量密码学和形式验证的静态知识，也可以很好地阅读已有模型；真正没有解决的是把这些知识可靠地编译成一个完整、语义忠实、威胁模型正确、验证属性完整、能够被具体 verifier 接受的形式对象。** 这也是解释任务能够达到 90 分以上，而生成和转换几乎崩溃的根本原因。citeturn24view2turn24view3

新闻中的“理解容易，生成和纠错难”应进一步修订成：

> **解释最成熟；有上下文的补全次之；编译级纠错已经具有一定实用性；从零生成仍很差；跨工具语义转换是最差的能力；真正的语义错误纠正也仍不可靠。**

这一描述比简单把 generation/transformation/correction 放在同一档更符合论文表 3。citeturn24view2

对于“700 个实例”，更完整的表述应是：

> **CrypFormBench 以 700 个验证过的形式模型参考文件为基础，覆盖 677 个密码方案、7 个验证器语言族和 160 种安全属性，并由其构造约 2,060 个五类任务级评测实例。** citeturn17view0

对于“Claude-3.5 最高只有 48.7”，这个数字是准确的，而且作者公开仓库重新按照论文 §4.5 计算仍得到 **48.7**。论文后来又测试了 GPT-5.1，得到 **39.2**，并没有推翻原有能力结构。citeturn19view0turn17view0

而对于 2026 年 9 月最新模型，GPT-5.6 Sol、Claude Opus 5/Fable 5、Gemini 3.1 Pro、DeepSeek V4-Pro、Grok 4.6 在通用推理、代码、网络安全和 agentic workflow 上已经发生了显著进步。citeturn21search1turn23view0turn20search5turn22search21turn22search3 但**截至目前没有公开 CrypFormBench 数据足以证明这些模型已经解决了形式密码分析的生成、跨工具转换和安全语义建模问题**；GPT-5.1 的同基准增量反而提醒我们，不应把通用 coding/GPQA/CTF 的进步线性外推为 formal-crypto DSL 的进步。citeturn17view0turn19view0

因此，以论文框架做最终量化归纳，我给出的结论是：

**知识储备：约 8/10。** 顶级解释得分 90+，说明协议、工具和代码模式知识已经相当丰富。citeturn24view2

**局部形式化辅助：约 6–7/10。** 补全和纠错最强达到 74.4/71.2，加 verifier 后已经能够产生生产力价值。citeturn24view2

**完整形式模型生成：约 2–3/10。** 论文最佳生成只有 24.2，可分析率 15%。citeturn24view2

**跨工具语义迁移：约 1/10。** 最好 13.3，八个基准模型为 0。citeturn19view0turn24view2

**自主安全证明/安全判定：约 2/10，且风险不对称。** 错误的形式模型可能得到完全正确的 verifier 证明；另一项 2026 年直接研究也发现模型会在漏掉攻击时保持极高置信度。citeturn15view2turn15view3

**LLM + verifier + RAG/examples + 多轮 agent 的现实潜力：约 7/10。** 这不是因为基础模型已经能独立证明协议，而是因为验证器可以把 LLM 最薄弱的“自由生成”变成一个有机器反馈约束的搜索—修复过程；CrypFormBench 的 few-shot 实验已经显示生成可分析率可以在非常简单的干预下提高约 3.8 倍，而 2026 年前沿模型又恰好在长时工具调用、自我检查和代码迭代上显著增强。citeturn24view5turn23view0turn21search1

所以，**现阶段最合理的技术目标不是“让大模型取代 Tamarin/EasyCrypt 专家”，而是“让大模型把专家从写 DSL、查语法、读 trace、做局部修复这些机械劳动中解放出来，同时让验证器和专家继续掌握最终的安全语义与证明责任”。** 这既是 CrypFormBench 数据真正支持的边界，也是截至 2026 年最新一代模型能力演进后仍然最稳妥的判断。 citeturn24view3turn17view0